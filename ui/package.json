{
  "name": "denki-yoho-ui",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "download-client": "wget http://127.0.0.1:8000/openapi.json -O openapi.json",
    "generate-client": "openapi-generator-cli generate -g typescript-fetch -i ./openapi.json -o ./src/api_client_tools"
  },
  "dependencies": {
    "element-plus": "^2.0.2",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.4.26",
    "@vitejs/plugin-vue": "^2.2.0",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "vue-tsc": "^0.29.8"
  }
}
